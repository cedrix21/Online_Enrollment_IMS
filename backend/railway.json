{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "rm -rf public/storage && php artisan storage:link && chmod -R 775 storage bootstrap/cache && /usr/bin/env nixpacks-php-server",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}