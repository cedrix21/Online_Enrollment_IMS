{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php artisan migrate --force && php artisan storage:link || true && chmod -R 775 storage bootstrap/cache && start-php-apache public/",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}