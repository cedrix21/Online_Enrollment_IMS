{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php artisan migrate:fresh --force --seed && php artisan storage:link || true && chmod -R 775 storage bootstrap/cache && heroku-php-apache2 public/",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}