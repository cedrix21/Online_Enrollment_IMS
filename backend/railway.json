{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php artisan migrate:fresh --force --seed && php artisan config:clear && php artisan serve --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/up",
    "restartPolicyType": "ON_FAILURE"
  }
}