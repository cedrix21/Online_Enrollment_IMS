{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php artisan migrate --force && php artisan storage:link || true && php artisan config:cache && php artisan route:cache && /usr/sbin/nginx -g 'daemon off;' & php-fpm",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}